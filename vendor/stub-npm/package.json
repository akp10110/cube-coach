{
  "name": "npm",
  "version": "6.0.0",
  "private": true,
  "description": "Empty stub. cubejs's package.json lists \"npm\" as a runtime dependency but never requires it (verified: no `require(\"npm\")` anywhere in its source) — this override replaces npm's real ~350-package dependency tree with nothing.",
  "main": "index.js"
}
